## 1. Org diagram empty-state placeholder

- [ ] 1.1 In `panopticon/diagrams.py`, replace `render_org_diagram`'s `if not any_section:` branch
      (currently "No cross-repo interface or dependency relationships yet.") with the placeholder: a link to
      `docs/setup-guide.md#4-initialize-a-child-repo`, then a fenced block (tagged with `fmt`) containing a
      six-node `?`-labeled hexagon ring in the diagram's format.
- [ ] 1.2 Update `tests/test_diagrams.py`'s existing empty-index assertion (currently checks for "No
      cross-repo interface or dependency relationships yet.") to assert the setup-guide link and the six `?`
      nodes are present instead.
- [ ] 1.3 Add a test confirming a populated compiled index still renders normal per-repo sections and no
      placeholder content (guards against the empty-state branch leaking into non-empty renders).

## 2. Template repo seed files

- [ ] 2.1 Run `write_org_diagram`'s render logic (or hand-write matching output) to create this repo's own
      `docs/architecture.md` as the placeholder content from task 1.1, so it's committed and ships with the
      template.
- [ ] 2.2 Rewrite this repo's `README.md` Overview section (the paragraph between `## Overview` and the `<p
      align="center"><img ...>` logo block): org-agnostic instance-appropriate boilerplate, then `[org
      architecture](docs/architecture.md)`, then a maintainer note instructing the org to replace the
      paragraph with their own description, then the existing logo image unchanged.
- [ ] 2.3 Leave `## How it works` and everything below it in `README.md` unchanged — it documents the system
      generally and applies to both template and instance repos.

## 3. Child repo README links

- [ ] 3.1 Add a rule to `.agents/skills/panopticon-doc-generation/SKILL.md` (alongside the existing
      `## Architecture diagram` back-link rule) instructing the skill to write/refresh two links at the top of
      `README.md`: `[{repo} architecture]({docs_location}/architecture.md)` above `[org
      architecture](https://github.com/{instance}/blob/{instance_default_branch}/docs/architecture.md#{repo})`.
- [ ] 3.2 Document the org-link derivation rule inline in that skill instruction: read
      `panopticon/config.json`'s `instance`/`instance_default_branch`/`repo`; fall back to a live `gh api`
      lookup when `instance_default_branch` is absent; stop and report rather than guess a branch name when
      both are unavailable — mirroring `panopticon/org_diagram_link.py`'s existing behavior.

## 4. Instance repo README link

- [ ] 4.1 Confirm (via the rewritten template Overview from task 2.2) that the instance repo's own
      `[org architecture](docs/architecture.md)` link is the only diagram link present — no per-child-repo
      links — since that content is seeded directly from the template and not generated by any instance-side
      tooling.

## 5. Verification

- [ ] 5.1 Run the full test suite (`python3 -m unittest discover tests` or per `docs/testing.md`) and confirm
      the updated `test_diagrams.py` assertions pass.
- [ ] 5.2 Manually render `write_org_diagram` against an empty instance fixture and inspect the output matches
      the spec's placeholder scenario (link + hexagon).
- [ ] 5.3 Update README.md and docs/spec.md to reflect any user-facing or architectural changes introduced by
      this change.
